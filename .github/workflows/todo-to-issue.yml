name: "Run TODO to Issue"
on:
  push:
    branches: [ main ]
jobs:
    build:
        runs-on: "ubuntu-latest"
        steps:
            - uses: "actions/checkout@v3"
            - name: "TODO to Issue"
              uses: alstr/todo-to-issue-action@c90d4c3f820f673bc3185ae9c3bf63e607010e36 # v4.10.2
